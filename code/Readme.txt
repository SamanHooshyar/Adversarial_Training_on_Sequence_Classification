#For basic lstm part.

Run the training.py file , adv and prox model part can be commented out

#For the Adv model part.

Saved basic lstm model can be loaded 
Change the epsilon values and epoch number as provided in the report.

#For the Prox model part

Saved basic lstm model can be loaded 
Change the epsilon values and epoch number as provided in the report.

#For the dropout and batch normalization part
set dropout to true and batch normalization to true correspondingly and run the prox lstm model for a given epsilon

#All the plots can be generated using plot.py/plot.ipynb file